# Telegram Bot "Чепуха" (Nonsense)

Русскоязычный многопользовательский Telegram bot для игры в "Чепуху" — весёлую партийную игру, где 2-6 игроков последовательно отвечают на забавные вопросы без видимости ответов друг друга. Результат — смешные и неожиданные истории!

## 🎮 Как это работает?

### Правила игры
1. **Создание комнаты**: Первый игрок начинает новую игру и получает 4-символьный код комнаты
2. **Присоединение**: Остальные игроки присоединяются по коду
3. **Вопросы**: Все по очереди отвечают на 6 вопросов в личных сообщениях:
   - "Какой?"
   - "Кто?"
   - "С кем?"
   - "Где?"
   - "Что делали?"
   - "Что с ними стало?"
4. **Истории**: После завершения каждый игрок получает 6 разных историй с ротированными ответами

## 📋 Команды

### Игра
- `/start` - Начать новую игру или присоединиться к существующей
- `/rules` - Показать правила игры
- `/reset` - Если игра сломалась (удаляет забагованную комнату)

### Информация
- `/history` - Показать последние 10 сохраненных историй
- `/bot_uptime` - Время запуска и время работы бота (МСК)
- `/stats` - Статистика бота (активные/завершенные игры, комнаты, игроки, истории)
- `/help` - Список всех доступных команд

### Администратор
- `/admin_stats` - Статистика для админа (полная информация по игроком, комнатам, истории)

## 🛠️ Установка и запуск

### Требования
- Python 3.11+
- Telegram Bot Token (получить от BotFather)

### Установка

```bash
# Клонировать репозиторий
git clone <repo-url>
cd telegram-bot-cepuha

# Установить зависимости
pip install -r requirements.txt

# Установить переменную окружения
export TELEGRAM_BOT_TOKEN="ваш_токен_бота"

# Запустить бота
python main.py
```

## 📦 Зависимости

- `python-telegram-bot==20.3` - Асинхронный клиент для Telegram Bot API
- `pytz` - Работа с часовыми поясами

## 💾 База данных

Проект использует SQLite3 с следующей структурой:

### games
Основная таблица с информацией об игре
- `game_id` - Уникальный идентификатор игры
- `room_code` - 4-символьный код комнаты (уникальный)
- `created_by` - ID создателя комнаты
- `status` - Статус: `waiting` (ожидание), `in_progress` (в игре), `completed` (завершена), `aborted` (таймаут), `reset` (удалена командой /reset)
- `current_question_idx` - Индекс текущего вопроса
- `created_at` - Дата создания

### game_players
Игроки в игре
- `game_id` - Связь с игрой
- `user_id` - Telegram ID игрока
- `username` - Никнейм Telegram (если есть)
- `first_name` - Имя игрока
- `is_admin` - Является ли создателем комнаты
- `awaiting_question_idx` - Какой вопрос игрок должен ответить
- `joined_at` - Время присоединения к комнате

### game_answers
Ответы игроков на вопросы
- `game_id` - Связь с игрой
- `question_idx` - Индекс вопроса
- `player_idx` - Индекс игрока
- `answer` - Текст ответа

### story_history
История всех историй
- `room_code` - Код комнаты
- `story_text` - Текст истории
- `created_at` - Дата создания

### bot_sessions
Логирование запусков бота
- `started_at` - Время запуска (МСК)

### user_activity
Отслеживание активности пользователей
- `user_id` - Telegram ID пользователя
- `username` - Никнейм Telegram
- `last_action` - Дата и время последнего действия
- `first_name` - Имя пользователя

### game_messages
Хранение ID сообщений Telegram для редактирования
- `game_id` - Связь с игрой
- `user_id` - Telegram ID получателя сообщения
- `message_id` - ID сообщения в Telegram (для редактирования)

## 🎯 Ключевые особенности

### Асинхронная обработка
- SQLite подключение закрывается ДО выполнения Telegram API операций
- Предотвращает блокировки и deadlock'и

### 5-минутный таймаут
- Если игрок не отвечает на вопрос 5 минут, игра отменяется
- Все игроки получают уведомление об исключении неактивного игрока
- Комната удаляется автоматически (статус: `aborted`)

### Ротированные истории
- История N начинается с игрока: `(N + question_idx) % num_players`
- Каждый игрок видит все истории с разными начальными позициями

### Автоматическое создание новой комнаты
- После завершения игры в той же комнате можно играть снова
- Все игроки сохраняют статусы администратора
- История сохраняется навечно в `story_history`

### Система удаления сломанных комнат
- Команда `/reset` помечает комнату как `'reset'` вместо полного удаления
- Все игроки и ответы сохраняются в статистике
- Админ может отследить удалённые комнаты в `/admin_stats`

### Администраторская статистика
- Команда `/admin_stats` показывает:
  - 🔵 Комнаты ожидания (статус `waiting`)
  - 🟣 Комнаты в игре (статус `in_progress`)
  - 🟢 Завершённые комнаты (статус `completed`)
  - 🔴 Комнаты прерваны по таймауту (статус `aborted`)
  - ⚫ Комнаты удалены командой /reset (статус `reset`)
- Для каждой комнаты отображается список игроков и админ (отмечен 👑)
- Список последних 10 активных пользователей

### Самопинг
- Бот пингует себя каждые 5 минут для поддержания активности
- Гарантирует непрерывную работу на серверах, которые отключают неиспользуемые приложения

## 🚀 Развертывание

### Локальная разработка (Replit)
Бот использует SQLite3 для локальной разработки автоматически.

### Production (Railway)
Бот поддерживает PostgreSQL для production развертывания:
1. Создать PostgreSQL add-on в Railway
2. Скопировать `DATABASE_URL` из переменных окружения
3. Бот автоматически определит PostgreSQL и будет его использовать
4. История всех игр сохранится при перезагрузке контейнера

**Примечание:** Без постоянного хранилища (PostgreSQL) все истории теряются при перезагрузке контейнера!

## 📝 Лицензия

MIT

## 👥 Автор

Создано для русскоязычной аудитории Telegram.
